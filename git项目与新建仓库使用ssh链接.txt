1.生成 ssh 密钥（终端执行）
ssh-keygen -t ed25519 -C "你的注册github邮箱"

2.查看公钥内容
cat ~/.ssh/id_ed25519.pub

3.复制全部公钥输出字符串，打开 GitHub → 头像→Settings→SSH and GPG keys→New SSH key，粘贴公钥保存

4. 验证连通性
ssh -T git@github.com
# 出现 Hi xxx! You've successfully authenticated 即成功